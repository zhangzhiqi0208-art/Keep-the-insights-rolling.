# 🎯 Echoball - FeedbackBridge 备份版本

## 📍 **文件夹位置**
```
/Users/didi/Echoball/
```

## ✅ **备份信息**

- **备份时间**: 2025-11-19 19:15
- **源文件夹**: `/Users/didi/zhangzhiqi/feedback-bridge/`
- **备份大小**: 约 711MB
- **备份状态**: ✅ 完整备份成功

## 🚀 **快速开始**

### **方法一：使用启动脚本（推荐）**
```bash
cd /Users/didi/Echoball
./start.sh
```

### **方法二：手动启动**

#### **启动后端服务器**
```bash
cd /Users/didi/Echoball/backend
source venv/bin/activate
python3 -m uvicorn main:app --host 0.0.0.0 --port 8001 --reload
```

#### **启动前端服务器**
```bash
cd /Users/didi/Echoball
python3 -m http.server 8000
```

### **访问地址**
- 🌐 前端页面: `http://localhost:8000`
- 📡 后端API: `http://localhost:8001`
- 📚 API文档: `http://localhost:8001/docs`

## ⚠️ **重要提示**

### **端口冲突处理**

如果原版本（feedback-bridge）还在运行，需要：

1. **停止原版本服务**：
```bash
kill $(lsof -ti:8000)  # 停止前端
kill $(lsof -ti:8001)  # 停止后端
```

2. **或使用不同端口**：
   - 修改 `start.sh` 中的端口号
   - 修改前端代码中的API地址

### **环境变量配置**

确保配置了 `DEEPSEEK_API_KEY`：
```bash
cd /Users/didi/Echoball/backend
cp env.example .env
# 编辑 .env 文件，添加你的 API 密钥
```

## 📁 **项目结构**

```
Echoball/
├── index.html              # 前端主页面
├── script.js               # 前端JavaScript（包含所有优化）
├── styles.css              # 样式文件
├── image/                  # 图片资源
├── icon/                   # 图标资源
├── templates/              # 模板文件
├── backend/                # 后端代码
│   ├── main.py            # FastAPI入口
│   ├── app/               # 应用代码
│   │   ├── api/           # API路由
│   │   ├── services/      # 业务逻辑
│   │   ├── config/        # 配置文件
│   │   └── utils/         # 工具函数
│   ├── prompts.json       # LLM配置
│   └── venv/              # Python虚拟环境
└── start.sh               # 快速启动脚本
```

## 🎯 **当前版本特点**

这是 **最新优化版本**，包含：

1. ✅ **影响分析精简** - 文本长度减少76.9%
2. ✅ **问题描述润色精简** - 规则减少33.3%
3. ✅ **问题描述要求简化** - 只要求"具体问题"
4. ✅ **硬编码路径修复** - 所有路径已改为相对路径
5. ✅ **所有最新优化和修复**

## 🔧 **修改建议**

这是一个独立的备份，可以在此基础上进行任何修改：

- ✅ 功能扩展
- ✅ UI优化
- ✅ 性能优化
- ✅ 新功能开发
- ✅ 实验性功能测试

**不会影响原版本！**

## 📝 **文件说明**

- `BACKUP_INFO.md` - 详细备份信息
- `start.sh` - 快速启动脚本
- `README.md` - 项目说明文档

## 🎉 **开始使用**

现在您可以在这个备份基础上进行任何修改和开发！

```bash
# 打开文件夹（在 Cursor 中）
# File -> Open Folder -> /Users/didi/Echoball

# 启动服务
cd /Users/didi/Echoball
./start.sh
```

---

**备份完成！** 🎊
